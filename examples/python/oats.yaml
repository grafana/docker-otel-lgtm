oats: 2
name: python rolldice serves traffic and lgtm is ready
fixture:
  type: compose
  template: lgtm
  compose_file: docker-compose.oats.yml
seed:
  type: app
input:
  - path: /rolldice
expected:
  custom-checks:
    - script: |
        set -eu
        curl -fsS "$OATS_GRAFANA_URL/api/health" >/dev/null
        curl -fsS "$OATS_APP_URL/rolldice" >/dev/null
