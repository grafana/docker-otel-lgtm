oats: 2
name: obi sidecar traffic yields traces across services
seed:
  type: app
expected:
  custom-checks:
    - script: |
        set -eu
        docker compose -f ../../ci/oats/docker-compose.lgtm.yml -f ./docker-compose.oats.yml logs --tail 200 generate-traffic java go python nodejs dotnet 2>&1 | grep -q 'rolling the dice'
